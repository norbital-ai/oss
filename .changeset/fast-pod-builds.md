---
'@norbital-ai/pod': patch
---

Keep isolated tenant builds below their latency and memory budgets by loading server and client
tooling only in the process that uses it, while preserving checker and migration validation.
