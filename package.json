{
	"name": "@norbital-ai/oss",
	"version": "0.0.1",
	"private": true,
	"packageManager": "pnpm@11.15.1",
	"engines": {
		"node": ">=26.0.0"
	},
	"scripts": {
		"build": "turbo build --concurrency=1",
		"lint": "turbo lint",
		"test": "turbo test",
		"test:ci": "pnpm --filter @norbital-ai/platform-utils test && pnpm --filter @norbital-ai/pod test:ci",
		"packages:build": "pnpm --filter @norbital-ai/pod... build",
		"check": "pnpm packages:build && pnpm deps:sync && pnpm templates:sync && pnpm lint && pnpm test && pnpm test:scripts && pnpm build && pnpm templates:check && pnpm templates:lock:check && pnpm publication:check",
		"check:ci": "pnpm packages:build && pnpm deps:sync && pnpm templates:sync && pnpm lint && pnpm test:ci && pnpm test:scripts && pnpm build && pnpm templates:check && pnpm templates:lock:check && pnpm publication:check",
		"check:packages:ci": "pnpm exec turbo run build --filter=...@norbital-ai/pod --concurrency=1 && pnpm exec turbo run lint --filter=...@norbital-ai/pod --concurrency=1 && pnpm test:ci && pnpm test:scripts && pnpm skills:check && pnpm publication:check",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"test:scripts": "node --test scripts/tests/*.test.mjs",
		"publication:check": "node scripts/validate-package-publication.mjs",
		"deps:sync": "node scripts/sync-injected-deps.mjs",
		"templates:sync": "node scripts/sync-templates.mjs",
		"templates:check": "node scripts/project-templates.mjs --check",
		"templates:lock": "node scripts/lock-templates.mjs",
		"templates:lock:check": "node scripts/lock-templates.mjs --check",
		"templates:lock:verify": "node scripts/lock-templates.mjs --check --verify-install",
		"templates:project": "node scripts/project-templates.mjs --update-local --output .tmp/template-revisions.json",
		"skills:generate": "node scripts/generate-skills.mjs",
		"skills:check": "node scripts/generate-skills.mjs --check",
		"docs:api": "typedoc --options typedoc.json && node scripts/generate-api-docs.mjs"
	},
	"devDependencies": {
		"@types/node": "^26.1.1",
		"@typescript/native-preview": "7.0.0-dev.20260707.2",
		"prettier": "^3.9.5",
		"prettier-plugin-svelte": "^4.1.1",
		"turbo": "^2.10.5",
		"typedoc": "0.28.20",
		"typedoc-plugin-markdown": "^4.12.0"
	}
}
