{
	"name": "@norbital-ai/oss",
	"private": true,
	"packageManager": "pnpm@11.15.1",
	"engines": {
		"node": ">=26.0.0"
	},
	"scripts": {
		"build": "turbo build",
		"lint": "turbo lint",
		"test": "turbo test",
		"ci:install": "pnpm install --frozen-lockfile --ignore-scripts && pnpm --filter @norbital-ai/pod... build && pnpm install --frozen-lockfile",
		"check": "pnpm lint && pnpm test && pnpm build && pnpm templates:check && pnpm publication:check",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"changeset": "changeset",
		"version-packages": "changeset version",
		"release": "pnpm check && changeset publish",
		"publication:check": "node scripts/validate-package-publication.mjs",
		"templates:check": "node scripts/project-templates.mjs --check",
		"templates:project": "node scripts/project-templates.mjs --update-local --output .tmp/template-revisions.json",
		"release:manifest": "node scripts/generate-platform-release.mjs",
		"pack:local": "pnpm build && node scripts/pack-local.mjs"
	},
	"devDependencies": {
		"@changesets/cli": "2.31.1",
		"@types/node": "^26.1.1",
		"@typescript/native-preview": "7.0.0-dev.20260707.2",
		"prettier": "^3.9.5",
		"prettier-plugin-svelte": "^4.1.1",
		"turbo": "^2.10.5"
	}
}
