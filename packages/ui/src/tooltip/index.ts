/**
 * Public tooltip API is the single Provider-backed wrapper only.
 * Do not import bits-ui Tooltip (or Root/Provider/Trigger) at call sites.
 */
export { default as Tooltip } from './tooltip.svelte';
